const ProductDetails = (x) => {
  console.log(x);
  return (
    <div>
      <h1 className="text-[50px]">
        This is the route that cathes all the routes after products
      </h1>
    </div>
  );
};

export default ProductDetails;
